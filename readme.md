# People Party test server

This is just a little test rig to provide a mock back end for people party testing

To run you will need to have `deno` installed and then run:

```
npm run start
```

Will run server on port 3000
